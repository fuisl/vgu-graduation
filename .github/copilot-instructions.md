Follow `AGENTS.md`; treat `docs/` as canonical project documentation.
For frontend work consult `docs/design/` and reuse established components/tokens.
For architecture changes consult `docs/architecture/` and `docs/adr/`.
Do not silently introduce new visual patterns, frameworks, infrastructure dependencies or data stores.
