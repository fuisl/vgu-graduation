# Docs
Planned Markdoc-powered documentation app rendering canonical Markdown under `/docs` and sharing design tokens/UI primitives where appropriate. The docs site is an interface over repository knowledge; Markdown remains the source of truth.
